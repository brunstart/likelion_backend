# POJO (Plain Old Java Object)
- 프레임워크나 라이브러리에 의존하지 않는 순수한 자바 객체
- 조건
  - 특정 프레임워크의 클래스를 상속받지 않음
  - 특정 프레임워크의 인터페이스를 구현하지 않음 (단, Serializable 같은 표준 자바 인터페이스는 허용)
  - 프레임워크 특화 애노테이션에 의존하지 않음 (단, @Override 같은 메타데이터 애노테이션은 허용)


## Spring에서의 POJO 활용
Spring 프레임워크는 POJO 기반 개발을 지원하는 대표적인 프레임워크

💡 Spring의 POJO 지원 방식

1. IoC (Inversion of Control)
   객체 생성과 의존성 주입을 Spring 컨테이너가 담당하지만, 비즈니스 로직은 순수 POJO로 작성.<br><br>

2. AOP (Aspect-Oriented Programming)
   트랜잭션, 로깅, 보안 등의 횡단 관심사는 AOP로 분리하여, POJO를 깔끔하게 유지.<br><br>

3. 애노테이션 기반 설정
   최소한의 애노테이션만 사용하여 POJO의 단순성을 유지<br><br>